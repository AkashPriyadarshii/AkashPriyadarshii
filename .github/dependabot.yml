version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns:
          - "*"
    # auto-accept SHA bumps for the exact pins we trust
    allow:
      - dependency-type: all
